name: orika-live-data
version: 1.1.0
description: "Orika CLI live data plugin. Connects to Orika WebSocket, stays live, and streams orders, deals, and positions to JSONL/CSV files. Credentials stay in local .env files."
author: Dipen / Hermes Agent
kind: general
provides_tools:
  - orika_stream_start
  - orika_stream_status
  - orika_stream_stop
